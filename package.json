{
  "name": "boplog",
  "version": "1.0.0",
  "private": true,
  "description": "A static, data-driven build log for kvnloo \u2014 public repos with authored commits",
  "scripts": {
    "serve": "python3 -m http.server 4173",
    "sync": "node scripts/sync-github.mjs",
    "validate": "node scripts/validate-data.mjs",
    "lint": "node --check app.js && node --check scripts/validate-data.mjs && node --check scripts/sync-github.mjs && node --check scripts/llmeo-probe.mjs && node --check scripts/llmeo-score.mjs",
    "check": "npm run lint && npm run validate",
    "llmeo:probe": "node scripts/llmeo-probe.mjs",
    "llmeo:score": "node scripts/llmeo-score.mjs",
    "llmeo:dry": "node scripts/llmeo-probe.mjs --dry-run"
  },
  "engines": {
    "node": ">=20"
  }
}
